language: elixir
elixir:
  - '1.6'
otp_release:
  - '20.3.8'
install:
  - cd bootstrap
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
script:
  - mix test
notifications:
  email: false
